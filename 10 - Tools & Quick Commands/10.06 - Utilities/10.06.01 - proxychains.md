
## Purpose
Route tools through SOCKS proxy when pivoting.

## Config File
`/etc/proxychains4.conf

Example:
`socks4 127.0.0.1 9050

## Usage

`proxychains nmap <target>

`proxychains smbclient -L //<target>

`proxychains crackmapexec smb <target>

## Notes
Used with SSH dynamic port forwarding.