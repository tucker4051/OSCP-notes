
# Best Scan Options

```
wpscan --url http://<url> --enumerate u,ap --api-token 4aMzBaIpvYYWJ5keAnjtEovyViaX357BW022iiPGSNM
```

`-u` - Enumerates users.
`-ap` - Enumerates all plugins.
`--api-token` - Optional, but provides access to the WPScan vulnerability database.